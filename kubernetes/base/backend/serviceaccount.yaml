apiVersion: v1
kind: ServiceAccount
metadata:
  name: backend
  namespace: production
  annotations:
    # IRSA — tells EKS which IAM role this service account can assume
    # Replace with the output of: terraform output irsa_backend_role_arn
    eks.amazonaws.com/role-arn: <irsa_backend_role_arn>
